apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: containerdevs-app
spec:
  selector:
    matchLabels:
      app: containerdevs-app # has to match .spec.template.metadata.labels
  serviceName: "containerdevs-app-headless"
  replicas: 3 # by default is 1
  template:
    metadata:
      labels:
        app: containerdevs-app # has to match .spec.selector.matchLabels
    spec:
      terminationGracePeriodSeconds: 10
      containers:
      - name: containerdevs-app-container
        image: postgres:13 # Replace with your stateful image
        ports:
        - containerPort: 5432
          name: db-port
        volumeMounts:
        - name: my-pvc
          mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
  - metadata:
      name: my-pvc
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: "standard" # Make sure this storage class exists
      resources:
        requests:
          storage: 1Gi
